name: Vulnerable Build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # ⚠️ Insecure: executes commit message as shell command
      - name: Run commit message as command
        run: bash -c "${{ github.event.head_commit.message }}"
